// var builder = WebApplication.CreateBuilder(args);
// var app = builder.Build();

// GROUPING
// var api = app.MapGroup("/api");

// // Users endpoints
// api.MapGet("/users", () => new[] { "John", "Jane" });
// api.MapGet("/users/{id}", (int id) => $"User {id}");
// api.MapPost("/users", (UserRequestDTO user) => $"Created user {user.Name}");

// // Posts endpoints
// api.MapGet("/posts", () => new[] { "firstPost", "secondPost" });
// api.MapGet("/posts/{id}", (int id) => $"Post {id}");
// api.MapPost("/posts", (PostRequestDTO post) => $"Created post {post.Title}");




// app.Run();


